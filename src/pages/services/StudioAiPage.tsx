import ServiceLayout from '../../components/ServiceLayout';
import { servicesData } from '../../data/servicesData';

const StudioAiPage = () => {
    return <ServiceLayout data={servicesData['studio-ai']} />;
};

export default StudioAiPage;
