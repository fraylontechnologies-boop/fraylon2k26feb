import ServiceLayout from '../../components/ServiceLayout';
import { servicesData } from '../../data/servicesData';

const PrototypingPage = () => {
    return <ServiceLayout data={servicesData['prototyping']} />;
};

export default PrototypingPage;
