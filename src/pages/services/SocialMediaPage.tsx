import ServiceLayout from '../../components/ServiceLayout';
import { servicesData } from '../../data/servicesData';

const SocialMediaPage = () => {
    return <ServiceLayout data={servicesData['social-media']} />;
};

export default SocialMediaPage;
